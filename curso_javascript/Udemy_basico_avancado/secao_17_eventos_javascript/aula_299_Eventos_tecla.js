window.addEventListener("keydown", (event) => {
    console.log(`Você clicou na letra ${event.key}`);
});

// window.addEventListener("keyup", (event) => {
//     console.log(`A tecla ${event.key} foi solta`);
// })